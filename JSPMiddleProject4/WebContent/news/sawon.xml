<?xml version="1.0" encoding="UTF-8"?>
<sawon> <!-- 데이터베이스의 테이블  -->
	<list> <!-- 데이터베이스의 ROW -->
		<name>홍길동</name>  <!-- 데이터베이스의 컬럼 -->
		<addr>서울</addr>
		<sex>남자</sex>
	</list>
	<list>
		<name>심청이</name>
		<addr>부산</addr>
		<sex>여자</sex>
	</list>
	<list>
		<name>이순신</name>
		<addr>통영</addr>
		<sex>남자</sex>
	</list>
</sawon>

<!-- 계층구조: DOM (Document Object Model): tree 형태로 만든다. -->

<!--
	 <XML>	  
	 - XML: eXtensible Markup Language  
	 - 사용자 정의 (지원하는 태그가 없음)
	 - 화면 출력X. 문서에 데이터를 저장하는 목적.  
	 - 모든 운영체제에 호환 가능.
	   => 언어와 상관없이 파싱(파싱: XML의 데이터를 읽는 것) ==> B2B
	   => 문서형 데이터베이스
	   => 단점: 문서가 길어지면 속도가 늦다.
	   	    사용자 정의 => 태그를 알아보기 어려울 수 있다 ==> 시맨틱(semantics) 태그 써라.
	   	    ※ 시맨틱 태그란?
	   	    - A semantic element clearly describes its meaning to both the browser and the developer.
	   	    - non-semantic elements ex) <div>, <object>, <embed>
	   	    - semantic elements ex) <video>, <audio>, <article>, <section>, <aside>, <table>
	 - XML을 이용하는 프로그램: MyBatis, Spring, Kotlin, AJAX, React, Vue, JSX(JavaScript+XML), TSX(TypeScript+XML) 
	 
	 0. 특징 
	  - XML은 다른 목적의 마크업 언어를 만드는 데 사용되는 다목적 마크업 언어.
	  - XML은 다른 시스템끼리 다양한 종류의 데이터를 손쉽게 교환할 수 있도록 해준다.
	  - XML은 새로운 태그를 만들어 추가해도 계속해서 동작하므로, 확장성이 좋다.
	  - XML은 데이터를 보여주지 않고, 데이터를 전달하고 저장하는 것만을 목적으로 한다.
	  - XML은 텍스트 데이터 형식의 언어로 모든 XML 문서는 유니코드 문자로만 이루어진다.
	  - [기타] XML 기반 언어: XHTML, SVG, RDF, RSS, Atom, MathML
	  	 
	 1. XML 만드는 방법
	  1) 태그
	  	(1) 여는 태그
	  	(2) 닫는 태그 
	  	 - 형식: <여는태그>데이터저장</닫는태그>
	  	        <여는태그 속성="데이터 저장">데이터저장</닫는태그>
	  	(3) 빈 태그(empty) 
	  	 - HTML에서 빈태그는 <br/>, <input/>, <img/>와 같이 속성으로 값을 지정하는 애들을 일컫음. 
	  	 - 일반적 태그: <태그>데이터 저장</태그> 
	  	 - 빈 태그: <태그 속성="데이터 저장"/> <=== 태그 사이에 데이터가 없으니까 empty 태그
	  	 - XML에서도 HTML과 마찬가지로 빈 태그들이 있다. 
	  	(4) 문법
	  	 - ⅰ. 최상위 태그를 가지고 있다.
	  	 - ⅱ. 여는 태그/닫는 태그가 반드시 존재한다. 
	  	     (물론 empty tag도 있음. 이런 경우는 <empty tag> 또는 <empty tag/> 이렇게 쓸 수 있음)
	  	 - ⅲ. 태그나 속성은 대소문자를 구분한다.
	  	 - ⅳ. 속성값은 반드시 "" 큰따옴표로 써야한다. 
	  	 - ⅵ. 키워드(xml)는 사용할 수 없다. 
	  	 - ⅶ. 태그는 공백이 포함되면 안된다.   
	  	(5) XML naming 규칙 from W3Schools
	  	 - 태그 이름은 대/소문자를 구분한다. 
	  	 - 태그 이름은 반드시 영문 혹은 _로 시작해야한다.
	  	 - 태그 이름에는 영문/숫자/-/_/.를 포함할 수 있다. 
	  	 - 태그 이름은 xml(XML,Xml 포함)으로 시작할 수 없다. 
	  	 - 태그 이름은 공백을 포함할 수 없다. 
	  2) 속성 
	  
	 2. DTD 파일
	  - DTD: Document Type Definition. 문서 타입 정의.

	 4. XML의 구성요소
	  1) 요소(elements)
	  2) 속성(attributes)
	  3) 엔티티(entities)
	  4) PCDATA
	  	 - PCDATA(Parsed Character DATA)란 XML 파서에 의해 분석될 ★문자 데이터★를 의미한다.
		 - 간단히 말해 XML 요소의 시작 태그와 종료 태그 사이에 위치한 텍스트를 PCDATA라고 생각하면 된다.
		 - 문법: <!ELEMENT 요소이름 (#PCDATA)>
	  5) CDATA
	 
	 
	 5. Java에서 파싱하는 방법 
	 
 -->
 
 <!-- <DOM>
 	 - 자바에서 파싱
 	 - [참고] DOM vs SAX(Spring, MyBatis)
 	 	1) DOM: 메모리에 저장, 제어 
 	 	2) SAX: 읽기 전용 ==> 수정/삭제/추가 불가 
 
 
  -->
 
 
 
 
 
 
 
 